name: Automatic Rebase

on: issue_comment

jobs:
  rebase:
    name: Rebase
    runs-on: ubuntu-latest
    steps:
    - if: github.event.comment.body == '/rebase2'
      uses: docker://cirrusactions/rebase:latest
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
